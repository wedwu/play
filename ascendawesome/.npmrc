# Scoped registry — all @ascend packages resolve from the internal registry.
# Auth token comes from the environment (CI secret) or user-level ~/.npmrc; never commit tokens.
@ascend:registry=https://registry.internal.example.com/
//registry.internal.example.com/:_authToken=${NPM_TOKEN}
